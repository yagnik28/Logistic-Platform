const User = require("./user");
const ActiveBooking = require("./activeBooking");
const PastBooking = require("./pastBooking");
const ActiveJob = require("./activeJob");
const PastJob = require("./pastJob");

module.exports = {
    User: User,
    ActiveBooking: ActiveBooking,
    PastBooking: PastBooking,
    ActiveJob: ActiveJob,
    PastJob: PastJob,
};